# Development-only broker config: anonymous, unencrypted, plain TCP on the default port.
# No auth, no TLS — acceptable here because this container is never exposed beyond the
# developer's own machine/network (see compose.yaml). Revisit before ever exposing this
# broker beyond a trusted local network.
listener 1883
allow_anonymous true
persistence false
