/**
 * Use cases for external product catalog search — fan-out to providers, caching, grouping —
 * plus the [org.gridmind.backend.catalog.application.ProductCatalogProvider] port those use
 * cases depend on. Concrete adapters implementing it live under
 * `catalog.infrastructure.provider`.
 */
package org.gridmind.backend.catalog.application
